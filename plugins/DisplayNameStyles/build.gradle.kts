version = "0.1.0"
description = "Display name styles"

android {
    namespace = "moe.lava.awoocord.dns"
}

aliucord {
    // Changelog of your plugin
    changelog.set("""
        # 0.1.0
        * Initial release >w<
    """.trimIndent())

    deploy.set(true)
}
