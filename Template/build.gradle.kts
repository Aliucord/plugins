version = "1.0.0"
description = "Template plugin"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release!
        """.trimIndent(),
    )

    deploy.set(false)
}
